    <div class="row-fluid">
        <div id="legend">
            <legend class="">New Reporter</legend>
        </div>
        <%= render "form", {:reporter => @reporter, :action => "update"} %>
    </div>